import styles from "./Slide.module.scss"

export default function FormElement() {
  return (
    <label className={styles.formBlock}>
      {/* <div className={styles.formBlockTitle}>Логин</div>
      <input
        className={styles.formBlockInput}
        type="text"
        name='nickname'
        value={form.nickname}
        onChange={inputChange}
      /> */}
    </label>
  );
}
