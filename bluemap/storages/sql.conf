storage: "sql"
  connection: {
    # Используем переменные окружения
    host: "${DB_HOST}"
    port: ${DB_PORT}
    database: "${DB_NAME}"
    username: "${DB_USER}"
    password: "${DB_PASSWORD}"
  }